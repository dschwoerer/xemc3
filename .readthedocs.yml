build:
  os: ubuntu-lts-latest
  tools:
    python: "3.12"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: requirements.txt
    - path: .

# No PDF (nbsphinx issue)
formats:
  - epub
  - htmlzip
